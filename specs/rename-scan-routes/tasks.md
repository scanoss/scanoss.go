<!-- Migrated from openspec/changes/rename-scan-routes/tasks.md -->

## 1. v3 SDK batch endpoint
- [x] 1.1 In `pkg/scanoss/scan_transport.go`, change `ServiceScan.endpoint` from `/v3/scan/batch` to `/v3/wfp/scan`
- [x] 1.2 Confirm both the upload POST (`uploadChunk`) and the poll GET (`scan.go` `Status` → `c2url`) derive from `ServiceScan.endpoint` (no other hardcoded `/v3/scan/batch`)

## 2. Legacy batch client (pkg/api)
- [x] 2.1 Confirm which backend `pkg/api` targets (v2 vs the renamed v3) — uses unprefixed paths (`/scan/direct`, `/scan/batch`)
- [x] 2.2 Updated `pkg/api/strategies.go` (`/scan/batch` → `/wfp/scan`) and `pkg/api/client.go` (`/scan/batch/{id}` → `/wfp/scan/{id}`) to match the rename
- [ ] 2.3 Open: confirm the legacy `pkg/api` backend also serves the unprefixed `/wfp/scan` (it may target a v2 host that hasn't renamed yet)

## 3. Verification
- [x] 3.1 `grep -rn "scan/batch" pkg/ cmd/` returns no unintended references (only autogenerated `types.gen.go` doc comments remain)
- [x] 3.2 `go build ./...`, `go vet ./...`, `go test ./...` green
- [ ] 3.3 Manual: scan against a backend serving `/v3/wfp/scan`; confirm upload + poll + result succeed

## 4. Docs & rollout
- [x] 4.1 Update any references to `/scan/batch` in doc comments (`scan_result.go`); regenerate `api/openapi.yaml` + `types.gen.go` from the renamed backend spec
- [ ] 4.2 Note in `CHANGELOG.md`
- [ ] 4.3 Coordinate release with the backend rename (lockstep — old/new paths are not interchangeable)
